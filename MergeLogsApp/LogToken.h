#pragma once
#include <iostream>
#include <string>
#include <fstream>
#include <sstream>
#include <memory>

namespace KMergeLogs {

    class LogToken {
        std::ifstream s;
        LogToken()
        {
            std::string ss;
            ss.length();
            ss.length();
            std::stringstream sd;
            std::shared_ptr<std::string> st;
            st->length();
            
        }
    };

}
